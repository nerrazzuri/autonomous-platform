Canonical logistics docs live under `apps/logistics/docs/`.
For post-refactor runtime smoke validation, start the app with `uv run python main.py` and use the canonical manual script at `apps/logistics/scripts/manual_e2e_smoke.sh`.

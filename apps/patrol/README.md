# Patrol Application Placeholder

The future patrol application will reuse the shared `core`, `quadruped`, `navigation`, `api`, `hardware`, and `runtime` modules introduced by this refactor.

Patrol-specific modules, workflows, and UI will be added later.

No patrol runtime or patrol business logic is implemented yet.
